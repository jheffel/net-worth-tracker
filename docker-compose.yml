services:
  backend:
    image: ghcr.io/jheffel/net-worth-tracker:latest
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
    volumes:
      - db_data:/app/db/

volumes:
  db_data:
